# Pion 开发约定

Pion 是基于 Electron、React、TypeScript、Vite 的 pi coding agent 桌面应用，采用 MIT 许可证。

## 先读哪里

- [map.md](map.md)：按功能定位源码与测试。
- [README.md](README.md)：使用说明。
- [docs/development.md](docs/development.md)：开发、构建和打包说明。
- [docs/coding-agent-platform.md](docs/coding-agent-platform.md)：平台设计参考；实际行为以当前源码为准。

## 工作规则

- 修改前检查 git 状态，保留用户已有改动，不覆盖、不顺手提交无关文件。
- 未经用户明确许可，不运行 typecheck、测试、构建、安装或 GUI 验证。可以编写测试、阅读源码及执行静态 diff 检查；汇报时区分已检查和未执行的验证。
- 推送、创建发布标签和发布安装包需要用户授权。
- 精确修改相关代码；不要编辑 node_modules、out、dist、coverage 等依赖或生成目录。
- 每次修改后都检查 AGENTS.md 和 map.md 是否仍与当前实现一致；涉及开发约定、架构、功能职责或文件路径变化时同步更新，防止文档过时。若无相关变化，无需制造无意义的文档改动。
- 新增、移动或删除功能文件时同步更新 map.md；避免把暂时的版本号、测试数量当作长期文档。

## 架构边界

- renderer 通过 typed IPC 与主进程通信；Git、文件系统和 Agent 后端操作由主进程执行。
- IPC 改动同时检查 src/shared/ipc.ts、src/shared/pion-api.ts、src/preload/index.ts 和 src/main/index.ts。
- pi 作为内置运行时，用户凭证仍归用户管理，不得打入安装包或提交。
- 会话子进程由编译后的 agent-runtime.mjs 通过 SDK 创建，提问工具使用 customTools 内置注册，不生成或安装提问插件。保留 RPC 协议、项目信任、会话替换和用户配置；系统 Node 执行的入口及共享块需解包到 app.asar.unpacked。
- 内置提问可用于普通/计划模式；取消、超时、无 UI 或中止均不得视作默认答案或授权。问答不替代工具权限检查，不触发写入检查点；答案会进入模型上下文和会话历史，不用此工具索取密码或密钥。
- 内置子代理默认关闭，由输入框按后端会话开启，重建后端后不自动恢复；开启须在构建模式空闲时，关闭中止当前子任务。子会话继承父级模型、项目和实时工具权限/检查点门控，仅使用父级可见的内置编码工具，不加载插件或递归委派；并发、轮数、时间与结果大小必须有界。全局子代理参数保存后仅对下一批生效；每批读取并冻结校验后的配置，不中断已有批次、不自动开启或重建后端。按钮须明确区分悬浮和开启状态，保留文字/图标标识。子工具 ID 隔离，取消信号传入权限交互，未获批准不得执行；子模型回复计入累计用量，不污染父级上下文占用。串行写入不等于文件冲突隔离，主 AI 仍需分配不重叠的文件范围。
- 项目信任和工具确认是策略层，不是 OS 沙箱，不能宣称任意 shell 命令被限制在项目目录内。
- 打包运行时路径转换位于 src/main/pi-runtime.ts，需兼顾 Windows 和 app.asar.unpacked。
- 原生半透明由主进程按平台设置，外观 IPC 只允许所属主窗口调用。Windows/macOS 使用原生材质；Linux 区分合成器透明与 KWin/X11 模糊请求，不把 CSS 模糊或 Wayland 回退宣称为通用桌面模糊。默认关闭，Linux 原生透明属性需要重启时不得自动重建窗口、中断会话或终端；保留高对比度及不透明回退。
- 内置终端由主进程的 node-pty 管理，只允许所属主窗口通过 typed IPC 操作；终端绑定打开时的真实项目/worktree 目录，不随当前会话静默切换 cwd。隐藏面板保留进程，结束终端或关闭窗口时清理。
- 内置终端使用当前系统用户权限，不受 Agent 工具确认规则限制，也不是项目目录沙箱。原生依赖须按 Electron ABI 重建，保留 node-pty 安装脚本和打包解包配置。

## UI 与会话

- 保留四套主题，使用已有主题变量；动效克制并兼容 prefers-reduced-motion。设置文案面向用户、保持简短；外观页使用“毛玻璃”开关，不堆叠合成器/API 技术说明或重复的实时预览，只在重启、不可用或透明回退等必要状态下给出一句提示。
- 面板让位动画保持组件和草稿身份；详情仅为短收起过渡保留 DOM，结束后释放，并支持快速反向开关。统计长条使用轻量底色反馈，不使用整条缩放造成视觉跳动。
- @ 参考菜单不得劫持回车：Enter 仍直接发送，Shift+Enter 换行，输入法组合输入期间不发送；选文件由用户显式点击触发。
- 区分历史分页、缓存恢复、实时增量与会话切换；避免整段重挂载。
- 任务面板使用独立的会话任务快照，不从当前历史页推导可见性。历史读取携带所选分支的最新任务状态，分页、跳转或加载错误不得把已有任务当成空列表；实时结果不依赖工具开始行仍在窗口内，重复结果须有界去重，异步恢复不得覆盖请求期间的新快照。明确 clear 才清空快照；真实会话切换、复制/分叉或删除活动会话时重置作用域，缓存恢复不能复活已清空任务。
- 工作区用嵌套分栏树计算面板矩形；模块保持固定 React 兄弟节点，拖动只调整 CSS 位置和尺寸，不通过重挂载重置会话、草稿或终端。布局与比例保存到本地，需兼容旧版迁移和损坏缓存；隐藏仅折叠投影，不删除布局节点。
- 阅读历史或使用跳转条后，实时输出、窗口焦点变化和面板尺寸变化不能强制拉回底部。
- 统计条与输入区域悬浮在连续会话消息区的顶部/底部，不缩短消息视口；按实际高度预留首尾滚动余量并避让历史导航，输入增长不得打断手动阅读，顶部余量变化需补偿阅读位置。统计详情在条下方同宽悬浮展开，不计入余量、不挤压历史跳转条或滚动条；任务和排队面板仍放在输入框上方的悬浮层。
- 次级资源分阶段加载只用于会话首次就绪；同会话的历史跳转、分页或加载错误不重置已启动阶段，不清空统计数据或断开实时订阅，只有真实项目/会话切换才重置。
- 自动跟随只应在用户原本位于底部时发生；用户离开末尾后，新消息、AI 输出和延迟图片/Markdown 布局不得拉回底部。用户手动返回真正末尾才恢复跟随（仅容忍像素取整，不把分页预取阈值当作到底）；已在末尾的向下输入即使不产生 scroll 事件也可恢复。程序化滚动后及时同步跟随状态。历史窗口页末不等于会话末尾，仍有后续历史时不得恢复实时跟随；历史分页追加与实时追加使用不同的 mutation。
- 分页调度与定位/跟随分开维护；在窗口边界无法产生 scroll 事件时，主动滚轮、触摸或翻页键输入仍可请求相邻页。两方向请求共享在途闸门，嵌套输出区优先消费自己的滚动，失败留在当前页等待新输入重试，切换项目/会话或跳转后不能接续旧窗口的填充请求。
- 加载/渲染期间用户滚轮、触摸、滚动条或翻页键输入应立即暂停跟随，不等待 scroll 事件；允许保留空白滚动占位，内容到达不强制缩短滚动范围或撤销用户向上滚动。程序化或无位移的 scroll 事件不能创建手动阅读状态、锁住临时占位或触发分页；没有主动滚动时，加载结束必须释放临时高度。详情折叠导致真实内容收缩时，按收缩量回收展开期间保留的高度；需观察内层内容，避免外层 min-height 遮蔽收缩，浏览器收缩钳制不得恢复实时跟随，也不一并清空无关加载占位。占位不是全量历史高度估算，用户明确返回底部、发起新历史定位或切换项目/会话时释放；历史定位须先清除旧窗口占位及旧手势状态，再按目标内容的真实滚动范围定位，不得等滚入空白末尾才回拉。
- 向前分页需按保留消息的实际布局位移补偿当前滚动位置，不能把保持 scrollTop 数值误当成保持阅读位置；加载中的用户位移必须保留，空白占位不能遮蔽新增内容高度，补偿产生的 scroll 事件不得连锁触发分页。
- 历史跳转定位与可见消息识别使用同一参考点；明确点击的消息 ID 在用户主动滚动前优先，不因相邻短消息或滚动边界限制被重新判成上一条。
- 历史索引按消息落盘/完成事件实时更新，不等待整轮空闲，不逐 token 扫描历史；合并在途期间的新请求，读取失败保留现有跳转条，更新不得重载消息区或重置用户浏览的导航范围。
- 会话后台运行、队列恢复、跨项目/worktree 切换和异步请求竞态都需要考虑。
- 新会话首次持久化后应主动推送所属项目的会话列表；后台项目推送不得替换当前项目列表，路径已分配不等于会话文件已可列出。
- 同一会话的压缩生命周期事件优先于异步状态快照；切换会话时重置事件状态，避免迟到快照覆盖压缩提示或让状态串会话。
- 上下文占用不是累计计费用量。压缩成功后作废旧请求的占用比例；没有压缩后实际用量时显示待更新，不伪造 0%，遥测快照不得覆盖更高 revision 的实时记录。
- 保留审查面板的渐入动画；优先用可见区域触发、动画结束后回收字符节点和减少重复渲染优化性能，不以删除动画替代优化。工具详情同样按可见小段创建动画字符，播放结束回收节点，实时输出只动画新增后缀；已播放字符不重复测量。长工具 diff 按页限制挂载行数，保留翻页查看全部差异，不通过静默截断或取消动画规避开销。
- 修改统计必须标明口径：Git 工作区（已暂存 + 未暂存，包含未跟踪文件）与本轮工具记录不同，不得混称。
- 运行统计优先展示已派发/执行中/收尾中的记录，排队及未派发便丢弃的消息不得抢占统计或计入等待时长。统计筛选必须在主进程和实时合并的条数限制之前进行，队列恢复查询仍保留排队记录；派发后的顺序依据实际启动/派发时间。有限窗口累计不得标成整个会话累计。
- 增加行为修复对应的回归测试；未经许可不要执行测试。

## 安装与发布

- scripts/install-local.sh 默认递增 patch 版本并构建，安装到 ~/.local/share/pion/app/，启动器为 ~/.local/bin/pion。
- 推送 v* 标签会触发 .github/workflows/release.yml，构建 Windows NSIS、Linux AppImage/deb 并发布。
- 每次发布后，总结本版本实际交付的新增、修复及重要行为变化，核对并填写 src/shared/release-notes.ts 的更新日志（供设置 → 关于 Pion 展示），按版本倒序排列，不写未经验证的性能或测试结论，不将仅本机安装的版本当作正式发布。
- 更新日志随安装包内置，因此打标签、打包前先准备本版本日志；发布完成后再核对最终发布内容并补充遗漏，同时同步 GitHub Release 说明。若打包后修改日志，不宣称已经生成的安装包包含该修改。
- 构建通过不等同于类型检查、测试或真机验证通过。
