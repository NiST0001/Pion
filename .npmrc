# Pion 项目级 npm 配置（覆盖用户全局设置）

# Electron 需要其 postinstall 脚本下载平台二进制，本项目重新启用脚本执行。
# （具体包白名单见 package.json 的 allowScripts 字段，npm 12+ 安全机制）
ignore-scripts=false

# Electron 二进制下载镜像（GitHub releases 直连不稳定时走 npmmirror）
electron_mirror=https://npmmirror.com/mirrors/electron/
